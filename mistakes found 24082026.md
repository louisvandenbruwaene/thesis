# Mistakes found, 24 August 2026

No unresolved confirmed defect remains.

One finding was rejected. The converted flow paths really are arc-disjoint because every membership arc is indexed by its hyperedge. The appendix leaves that membership-arc step implicit, so the passage is correct but compressed.

The remaining observations are accepted tradeoffs. Whole-source cache hashing favours stale-cache safety. The appendix gives the portable block command while the transcript records the temporary dependency path used for the archived run. Capped public helpers suppress an older out-of-range diagnostic. The automorphism identity check remains an assertion because downstream division still fails immediately if a zero count appears under optimization.

Verification passed: 103 tests with 6 expected skips, three guard tests under `python -O`, the optimized witness sweep for $2\le n\le14$ and $2\le m\le6$, and all previously recorded document builds.
