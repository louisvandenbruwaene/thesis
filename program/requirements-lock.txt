# The exact environment the reported values, figures and test suite were last
# verified in (2026-09-02, a from-scratch rebuild of all 529 machine values).
# Reproduce it FROM THE REPOSITORY ROOT, which is where the environment lives
# and where every command the thesis prints expects to find it as ../.venv:
#
#     python3 -m venv .venv
#     .venv/bin/python3 -m pip install -r program/requirements-lock.txt
#
# Running this from program/ instead would build the environment one level too
# deep and leave ../.venv missing.
#
# Python 3.14.6 on macOS (darwin, arm64).
#
# Core, required.
numpy==2.4.6
scipy==1.18.0

# MILP certifier (prove_directed_multigraph / prove_integral_arc_bound).
pulp==3.3.2

# Optional, presentation only: matplotlib renders the figures, networkx supplies
# the Gomory-Hu tree behind one figure. The test suite skips cleanly without both.
matplotlib==3.11.0
networkx==3.6.1
