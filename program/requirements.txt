# Dependencies for erdos915_unified.py.
#
# Core (required): the model, the connectivity checker, the search, the
# enumeration, the random-model sampling, and the built-in self-test all run on
# these two alone, because every connectivity measure is one scipy integer
# maximum flow on a capacity matrix.
numpy>=2.0
scipy>=1.13

# Optional, presentation only. matplotlib renders the figures (make_figures.py);
# networkx supplies the Gomory-Hu tree behind one figure. Everything else, including
# the test suite, runs without them.
# matplotlib>=3.8
# networkx>=3.0

# The exact environment in which the reported runs and the test suite passed is
# recorded in requirements-lock.txt. The ranges above are what the program
# actually needs; the lock file is what it was last verified against.
